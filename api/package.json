{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"Build api ok\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "prisma": "^5.0.0"
  }
}
